import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.intercomplatform.resident',
  appName: 'Intercom Resident',
  webDir: 'dist',
};

export default config;
